{
  "name": "show-version-number-nausaf",
  "version": "1.0.0",
  "description": "Prints 'Hello World!' and the version number from package.json when run from command line using npx",
  "bin": "./cli.js",
  "type": "module",
  "scripts": {
    "prepare": "husky",
    "release": "semantic-release"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@commitlint/cli": "^19.5.0",
    "@commitlint/config-angular": "^19.5.0",
    "@semantic-release/changelog": "^6.0.3",
    "@semantic-release/exec": "^6.0.3",
    "@semantic-release/git": "^10.0.1",
    "husky": "^9.1.6",
    "semantic-release": "^24.1.2"
  }
}
